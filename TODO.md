# Navbar Navigation Fixes — TODO

1. ✅ Fix order.html nav/footer links: replace absolute `href="/"` paths with relative `index.html` paths so links work when opened as a file.
2. ✅ Fix order.js hamburger: toggle `open` class on the hamburger button so the icon animates into an "X".
3. ✅ Fix order.js mobile menu: close the menu when a nav link is clicked.
4. ✅ Fix index.html: remove hardcoded `active` class from the Home nav link so the scrollspy manages active-state switching cleanly.
5. ✅ Verify all nav links on both pages switch correctly (server started via `npm start`, running at http://localhost:4000).
</content>
